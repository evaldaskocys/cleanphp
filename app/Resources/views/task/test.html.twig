{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            <ol class="breadcrumb">
                <li><a href="{{ path('task_index') }}">Back to the list</a></li>
            </ol>
            <h1>Task assign to test</h1>
            {{ form_start(test_form) }}
            {{ form_widget(test_form) }}
            <input type="submit" value="Save" class="btn btn-success pull-right" />
            {{ form_end(test_form) }}
        </div>
        <div class="col-md-3"></div>
    </div>
{% endblock %}
